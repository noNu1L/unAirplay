===========================================
  unAirplay - Version Log / 版本日志
===========================================

This version log is intended for regular users.
Technical implementation details are not included.

此版本日志面向普通用户阅读，不包含技术实现细节。

---

v1.1.5 (2026-05-28)
-------------------

1. Pause Auto-Stop / 暂停超时自动停止

   - Automatically clears playback status after a configurable idle period in paused state
   - Default disabled (PAUSED_AUTO_STOP_TIMEOUT=0), set in minutes via config.py or environment variable
   - When triggered, the device state changes to STOPPED, which may be broadcast to your DLNA client
   - 暂停播放后，可配置超时自动清空播放状态
   - 默认关闭（PAUSED_AUTO_STOP_TIMEOUT=0），通过 config.py 或环境变量设置分钟数
   - 触发后设备状态变为已停止，可能会广播到你的 DLNA 客户端

2. Improved AirPlay Connection Error Logging / 改进 AirPlay 连接错误日志

   - AirPlay device connection failures now show which device was affected
   - AirPlay 设备连接失败时现在会明确显示设备名称


v1.1.4 (2026-05-12)
-------------------

1. Environment Variable Configuration / 环境变量配置支持

   - All configuration options now support environment variables for easier Docker deployment
   - Supports DEBUG, HTTP_PORT, WEB_PORT, SSDP_PORT, DEVICE_SUFFIX, AIRPLAY_EXCLUDE, AIRPLAY_OFFLINE_THRESHOLD, ENABLE_SERVER_SPEAKER, etc.
   - 所有配置项新增环境变量支持，便于 Docker 部署
   - 支持 DEBUG、HTTP_PORT、WEB_PORT、SSDP_PORT、DEVICE_SUFFIX、AIRPLAY_EXCLUDE、AIRPLAY_OFFLINE_THRESHOLD、ENABLE_SERVER_SPEAKER 等环境变量

2. Bug Fix: Playback Status / 修复：播放状态异常

   - Fixed playing status not updating correctly in some cases
   - Fixed playback stalling when network resource acquisition fails
   - 修复部分场景下播放状态更新不正确的问题
   - 修复网络资源获取失败时播放中断的问题

3. Bug Fix: Stream Switching / 修复：切歌异常

   - Fixed RTSP session conflict (400 error) when switching songs rapidly
   - 修复快速切歌时 RTSP 会话冲突导致 400 错误的问题

4. Custom Response Header for vDLNA Integration / 添加自定义响应头用于 vDLNA 联动

   - Added custom response header exposing the Web UI address for integration with vDLNA project
   - Enables vDLNA to discover and link to unAirplay's Web DSP settings panel
   - 添加自定义响应头，对外暴露 Web 界面地址，用于与 vDLNA 项目联动
   - vDLNA 可发现并关联至 unAirplay 的 Web DSP 设置面板

v1.1.3 (2026-02-08)
-------------------

1. Bug Fix: Audio Glitch on Song Switch / 修复：切歌时的音频杂音

   - Fixed old audio briefly playing when switching to next song
   - 修复切换到下一首歌曲时短暂播放旧音频的问题

2. FFmpeg Installation Guide & Startup Check / FFmpeg 安装指南与启动检查

   - Improved installation guide with clearer instructions
   - Check FFmpeg installation before startup, show installation guide if not found
   - 改进安装指南，提供更清晰的说明
   - 启动前检查 FFmpeg 安装状态，未安装时显示安装指引

3. Docker Image Optimization / Docker 镜像优化

   - Reduced Docker image size
   - 减小 Docker 镜像体积


v1.1.2 (2026-02-07)
-------------------

1. AirPlay Device Offline Detection / AirPlay 设备离线检测

   - Devices offline for consecutive scans will be automatically removed
   - Configurations automatically restored when device comes back online
   - 设备连续多次扫描未检测到将自动删除虚拟设备
   - 设备重新上线时自动恢复配置

2. Bug Fix: Device Reconnection / 修复：设备重连问题

   - Fixed playback errors after device reconnects
   - 修复设备重连后的播放错误


v1.1.1 (2026-02-06)
-------------------

1. Streaming Audio Support / 流媒体音频支持

   - Added support for streaming audio sources (e.g., online radio)
   - 添加流媒体音频源支持（如网络电台）

2. AirPlay Device Exclusion / AirPlay 设备排除

   - Added AIRPLAY_EXCLUDE configuration to exclude devices by IP or name
   - 添加 AIRPLAY_EXCLUDE 配置，可按 IP 或名称排除设备

3. Bug Fix: Metadata Display / 修复：元数据显示

   - Fixed missing song metadata in some cases
   - 修复部分情况下歌曲元数据缺失的问题

4. Bug Fix: Multi-client Switching / 修复：多客户端切换

   - Fixed errors when switching between multiple DLNA clients
   - 修复在多个 DLNA 客户端之间切换时的错误


v1.1.0 (2026-02-04)
-------------------

1. Project Rename / 项目重命名

   - DLNA-To-Airplay -> unAirplay

2. Web UI Redesign / 页面重新设计

   - More minimalist design with integrated DSP settings
   - 更简约的设计，DSP 设置一体化

3. DSP Enhancement / DSP 增强

   - EQ & Spectral enhancement now support IIR/FFT/FIR modes
   - Spectral gain range expanded from 1.0-2.0 to 0.5-2.0
   - EQ 和频谱增强现在支持 IIR/FFT/FIR 三种模式
   - 频谱增强调整范围从 1.0-2.0 扩展至 0.5-2.0

4. Cache System / 缓存系统

   - Ensures continuous playback even when stream URL is interrupted
   - 保证音乐 URL 断流情况下仍能继续播放

5. Bug Fix / 修复

   - Fixed UI still showing "playing" status after track completion
   - 修复歌曲播放完成后页面仍显示播放中的问题

6. UI Enhancement / 界面优化

   - Playing devices now prioritized at the top of device list
   - 播放中的设备优先排列在设备列表顶部


v1.0.0 (Initial Release / 初始版本)
-----------------------------------

1. DLNA to AirPlay / DLNA 到 AirPlay

   - Stream audio from DLNA clients to AirPlay devices
   - 将 DLNA 客户端的音频推送到 AirPlay 设备

2. DLNA to Server Speaker / DLNA 到本地扬声器

   - Play audio on the server's local speaker
   - 在服务器本地扬声器播放音频

3. Music App Compatibility / 音乐应用兼容

   - Optimized DLNA requests for NetEase Cloud Music, QQ Music, Kugou Music, Kuwo Music, Migu Music
   - 针对网易云音乐、QQ音乐、酷狗音乐、酷我音乐、咪咕音乐的 DLNA 请求进行适配优化

4. DSP Features / DSP 功能

   - EQ, Spectral Enhancement, Dynamic Range Compression, Stereo Widening
   - EQ、频谱增强、动态范围压缩、立体声拓宽
